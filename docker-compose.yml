version: "3.9"
services:
  sleeper-web:
    build: .
    image: sleeper-web:latest
    environment:
      - PORT=8000
    expose:
      - "8000"  # internal only, no host binding
    command: >
      uvicorn webapp:app --host 0.0.0.0 --port 8000
